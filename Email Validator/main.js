// --- DOM Elements ---  
    // App elements
    const tabs = document.querySelectorAll('.tab');
    const tabContents = document.querySelectorAll('.tab-content');
    const restartButton = document.getElementById('restartApp');

    // Single tab elements
    const emailInput = document.getElementById('emailInput');
    const validationIcon = document.getElementById('validationIcon');
    const validationMessage = document.getElementById('validationMessage');
    const validateButton = document.getElementById('validateButton');
    const result = document.getElementById('result');

    // Bulk tab elements
    const bulkEmailInput = document.getElementById('bulkEmailInput');
    const bulkValidationMessage = document.getElementById('bulkValidationMessage');
    const validateBulkButton = document.getElementById('validateBulkButton');
    const bulkResults = document.getElementById('bulkResults');
    const copyValidBtn = document.getElementById('copyValidBtn');
    const progressBarContainer = document.getElementById('progressBarContainer');
    const progressBar = document.getElementById('progressBar');
    const validCountDisplay = document.getElementById('validCount');
    const invalidCountDisplay = document.getElementById('invalidCount');
    const totalCountDisplay = document.getElementById('totalCount');

    // Domain tab elements
    const domainInput = document.getElementById('domainInput');
    const domainValidationIcon = document.getElementById('domainValidationIcon');
    const domainValidationMessage = document.getElementById('domainValidationMessage');
    const validateDomainButton = document.getElementById('validateDomainButton');
    const domainResult = document.getElementById('domainResult');
    const checkMX = document.getElementById('checkMX');


// Write a code...
